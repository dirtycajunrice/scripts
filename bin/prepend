#!/bin/bash

# This miniscript prefixes the current local
# date and time, or error, or both to every 
# line that is piped through it.

if [ $1 = 'error' ]; then
	while read -r line; do
			echo "$(date): [ERROR] ${line}"
		done
else
	while read -r line; do
		echo "$(date): ${line}"
	done
fi
